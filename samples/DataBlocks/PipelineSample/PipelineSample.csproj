<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <!-- Clear the repo-inherited plural TargetFrameworks so this stays single-target (runnable without a framework flag). -->
        <TargetFrameworks></TargetFrameworks>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
    </PropertyGroup>

    <ItemGroup>
      <ProjectReference Include="..\..\..\src\CloudMesh.DataBlocks\CloudMesh.DataBlocks.csproj" />
    </ItemGroup>

</Project>
